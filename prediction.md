# Part 1 Prediction:

Based on UTF-8 byte counts, I predict RU/EN ≈ 1.9× and KK/EN ≈ 2.1× in tokens, since Cyrillic characters take twice as many bytes as Latin ones.

# Core Task 3 Prediction:

I predict JSON will cost more tokens than prose in all three languages, since braces, quotes, colons, and field names add fixed overhead regardless of language..
